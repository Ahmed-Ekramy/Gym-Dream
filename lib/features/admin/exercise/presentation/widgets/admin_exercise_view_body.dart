import 'package:flutter/material.dart';

class AdminExerciseViewBody extends StatelessWidget {
  const AdminExerciseViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Column(
        children: [
          Text('ExerciseViewBody'),
        ],
      ),
    );
  }
}
